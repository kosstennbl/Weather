export type DayInfo = {
    date: string
    weekday: string
    midTemp: number
    rainProb: number
}

export type HourInfo = {

}

export type WeatherSource = {
    name: string
    address: string
}